name: CI Pipeline for Web Application

on:
  push:
    branches:
      - main
      - dev

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout source code
        uses: actions/checkout@v3

      - name: Run HTML build check
        run: echo "HTML project build successful"

      - name: Run basic tests
        run: echo "Basic tests passed successfully"
